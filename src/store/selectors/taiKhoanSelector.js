// import { createSelector } from "@reduxjs/toolkit";

// export const listTKSelector = (state) => state.taiKhoan.list;
// export const filterTK = (state) => state.taiKhoan.filter;

// export const listKhuVucAll = createSelector(
//     listTKSelector,
//     filterTK,
//     (list, filterTK) => {
//         return list.filter((item) => {
//             return (filterTK) ? item.idKhuVuc.includes(filterTK) || item.tenKhuVuc.toLowerCase().includes(filterTK)  : item;
//         });
//     }
//   );
  